import Button from 'react-bootstrap/Button';

export function ULGoogleButton() {
    return (
    <div className="d-grid gap-2">
      <Button variant="outline-primary" size="lg">
        Google Oauth tobe setup
      </Button>
    </div>
  );
}